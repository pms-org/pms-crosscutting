apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pms-crossref.fullname" . }}
  labels:
    {{- include "pms-crossref.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "pms-crossref.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pms-crossref.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            {{- toYaml .Values.env | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}